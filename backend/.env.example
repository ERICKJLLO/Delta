# Puerto en el que corre el servidor backend
PORT=3001

# Secreto para firmar tokens JWT (Cambiar por uno seguro en producción)
JWT_SECRET=tu_secreto_super_seguro_aqui

# Ruta del archivo SQLite para la base de datos (relativa a backend/)
DB_PATH=./data/delta.db
